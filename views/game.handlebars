    <div class="container">
        <img src="{{this.dataValues.image}}" alt="..." width="40%" height="40%" class="game">
        <div class="title">
            <p>{{this.dataValues.title}}</p>
        </div>
        <div class="buttons">
            <button class="addLibrary">+ Add Game To Library</button>
            <button class="addWishlist">+ Add Game To Wishlist</button>
        </div>
    </div>